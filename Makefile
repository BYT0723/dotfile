update:
	cp ~/.profile ./
	cp ~/.bash_profile ./
	cp -r ~/.config/mpd ./
	cp -r ~/.config/mpv ./
	cp -r ~/.config/ncmpcpp ./
	cp -r ~/.config/neofetch ./
	cp -r ~/.config/nnn ./
	cp -r ~/.config/osdlyrics ./
	cp -r ~/.config/tmux ./
	cp ~/.config/screenkey.json ./
